# 受控实验方案（自动生成）

- 生成时间：2026-04-02 16:47:21
- 目的：验证“将充电时间份额从其余59区间替代到目标区间”是否改善下一循环放电容量。
- 替代幅度：每个试验臂固定 `+5pp`（即 +0.05 时间份额）。

## 1. 分组设计
- 对照组：维持当前充电策略，不做份额替代。
- 试验组1：将总充电时间中 `+5pp` 替代到 `cross_bin=50`（s3_r2_t5），时间来源为其余59区间总池；预计方向：提升下一循环容量。
- 试验组2：将总充电时间中 `+5pp` 替代到 `cross_bin=41`（s3_r1_t1），时间来源为其余59区间总池；预计方向：降低下一循环容量。
- 试验组3：将总充电时间中 `+5pp` 替代到 `cross_bin=44`（s3_r1_t4），时间来源为其余59区间总池；预计方向：提升下一循环容量。

## 2. 执行约束
- 保持每个cycle总充电时间不变，仅做区间份额重分配。
- 满足现有SOC窗口与安全约束（温度、电流、截止条件）。
- 若执行中触发安全阈值，立即回退到对照策略。

## 3. 观测与终点
- 建议观测窗口：每组至少30个连续cycle。
- 主要终点：`q_discharge_{t+1}` 相对对照组的均值差。
- 次要终点：30-cycle容量下降斜率、异常跳变事件率。

## 4. 停止条件
- 任一试验组出现连续3个cycle容量显著下降且伴随安全告警。
- 关键安全指标超限（温度或电流保护触发）。