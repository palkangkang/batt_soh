# 受控实验方案（自动生成）

## 0. 方案摘要
- 生成时间：2026-04-07 16:42:19
- 目标：验证“将充电时间份额从其余59区间替代到目标区间”对下一循环放电容量的因果影响。
- 干预幅度：每个试验臂固定 `+5pp`（`+0.05` 时间份额），对照组不干预。
- 随机化单位：`policy + cell_code`，避免同一电芯跨组污染。
- 分层建议：按 `policy三元参数`、`q_t` 分位、`is_abnormal_cell` 分层后再随机化。

## 1. 研究假设
- 原假设 H0：在固定总充电时间下，将 `+5pp` 份额替代到目标区间，不改变 `q_discharge_{t+1}`。
- 备择假设 H1：上述替代会改变 `q_discharge_{t+1}`，方向由观测因果估计给出。

## 2. 试验臂设计
- 对照组：维持现有充电策略，不做份额重分配。
- 试验组1：目标 `cross_bin=50`（s3_r2_t5），将总充电时间 `+5pp` 替代至该区间，来源为其余59区间总池；观测预测方向：提升容量，估计量级约 `0.000703 Ah/5pp`。
- 试验组2：目标 `cross_bin=41`（s3_r1_t1），将总充电时间 `+5pp` 替代至该区间，来源为其余59区间总池；观测预测方向：降低容量，估计量级约 `-0.000383 Ah/5pp`。
- 试验组3：目标 `cross_bin=44`（s3_r1_t4），将总充电时间 `+5pp` 替代至该区间，来源为其余59区间总池；观测预测方向：提升容量，估计量级约 `0.000540 Ah/5pp`。

## 3. 干预实施规则
- 每个 cycle 保持总充电时间不变，仅调整60区间内部份额分配。
- 在 SOC 与安全约束内执行替代：不突破温度、电流、截止电压等保护边界。
- 若当 cycle 无法满足 `+5pp` 目标（可行域不足），记录为 protocol deviation，不强制外推。

## 4. 终点与统计分析计划
- 主要终点：`q_discharge_{t+1}` 相对对照组的均值差（按 `policy+cell` 聚类稳健标准误）。
- 次要终点：30-cycle 衰减斜率、异常跳变事件率（`dt_s > 3600`）。
- 建议分析：混合效应/聚类稳健回归，固定效应含 `policy三元参数` 与 cycle 阶段。
- 建议最小观测窗口：每组至少30个连续 cycle。

## 5. 停止规则与安全约束
- 任一试验组出现连续3个 cycle 容量显著下降且伴随安全告警时，触发停臂评估。
- 温度、电流任一保护触发达到预设阈值，立即回退到对照策略。
- 发生 protocol deviation 的样本单独标注，按 ITT 与 PP 两套口径同时分析。

## 6. 证据闭环
- 本方案用于将“观测因果估计”转化为“可操作策略证据”。
- 最终策略上线以受控实验结果为准，不以单次观测估计直接替代。