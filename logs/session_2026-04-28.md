# Session 2026-04-28

## Prompt

PLEASE IMPLEMENT THIS PLAN:

# 生成 Cursor Rules，保持 AGENTS.md 不变

新增 `.cursor/rules/*.mdc`，从现有 `AGENTS.md` 拆解生成 Cursor 规则文件，并保持 `AGENTS.md` 原样不变；实施时追加记录到 `logs/session_2026-04-28.md`。

## Response

直接执行：

- 新增 `.cursor/rules/00_collaboration_and_safety.mdc`
- 新增 `.cursor/rules/10_project_structure_and_data.mdc`
- 新增 `.cursor/rules/20_modeling_training_evaluation.mdc`
- 新增 `.cursor/rules/30_task_templates.mdc`
- 新增 `.cursor/rules/40_session_logging_and_delivery.mdc`
- 保持 `AGENTS.md` 不变。
- 新增本日志文件记录本轮 prompt 与 response。

深度交互：

- 该实现保留 `AGENTS.md` 作为原始规范，同时为 Cursor 提供结构化 Project Rules。
- 后续修改规则时，需要同步维护 `AGENTS.md` 与 `.cursor/rules/*.mdc`，避免两套规则漂移。

## Prompt

PLEASE IMPLEMENT THIS PLAN:

# Cursor 规则文件中文化

仅修改 `.cursor/rules/*.mdc` 这 5 个 Cursor 规则文件，不修改 `AGENTS.md`。将规则正文、Markdown 标题、说明文字、列表项、`description` 的值翻译为简体中文；保持 Cursor 必需格式、字段名、布尔值、glob 路径、文件名、代码标识和项目事实原样；追加本轮变更记录到 `logs/session_2026-04-28.md`。

## Response

直接执行：

- 将 `.cursor/rules/00_collaboration_and_safety.mdc` 中文化。
- 将 `.cursor/rules/10_project_structure_and_data.mdc` 中文化。
- 将 `.cursor/rules/20_modeling_training_evaluation.mdc` 中文化。
- 将 `.cursor/rules/30_task_templates.mdc` 中文化。
- 将 `.cursor/rules/40_session_logging_and_delivery.mdc` 中文化。
- 保持 Cursor frontmatter 字段名、`globs`、`alwaysApply` 与布尔值不变。
- 保持 `AGENTS.md` 不变。
- 追加本日志记录。

深度交互：

- 本次中文化不是机械逐字翻译，而是保留原规则含义并转写为更适合当前中文协作语境的规则文本。
- 后续若修改 `AGENTS.md` 或 Cursor 规则，应同步维护两边，避免规则漂移。
