[Prompt]
开始执行吧。
[Response]
已执行方案A：新增 scripts/estimate_causal_initial_rate_effect.py，运行并输出到 outputs/analysis/causal_initial_rate_effect（目录命名不含200）。生成全局+1C效应、分阶段+1C效应、剂量反应曲线、诊断与报告。
[Prompt]
PLEASE IMPLEMENT THIS PLAN:
## 中文报告与可解释图增强计划（方案A改版）

### 直接执行：摘要
- 将当前英文报告改为中文输出，保留原文件名与目录，不在命名中显式使用 `200`。
- 在报告中新增“参数来源详解”，逐项说明每个参数来自哪张表、哪一列、通过什么公式/步骤得到。
- 增加辅助解释图片，并保证每张图都配套：`X轴含义`、`Y轴含义`、`关键性结论`。
- 保持当前识别口径不变：`GPS + AIPW`、滚动窗口、`VARCHARGE` 排除、全局与分阶段 `+1C` 效应同时输出。

### 直接执行：关键改动
- 在脚本 [estimate_causal_initial_rate_effect.py](C:/Users/pal/projects/batt_soh/scripts/estimate_causal_initial_rate_effect.py) 中做以下增强：
1. 报告模板改为中文（标题、章节、表头、图注、结论语句全部中文）。
2. 新增参数来源字典构建逻辑，输出 `report_parameter_sources.csv`，字段至少包含：
- `parameter_name`
- `section`
- `source_file`
- `source_columns`
- `formula_or_rule`
- `notes`
3. 报告新增“参数来源详解”章节：
- 对报告正文出现的参数逐项覆盖（Setup、主效应、分阶段表、诊断指标、复现命令参数）。
4. 新增并输出辅助图（在现有两张图基础上补齐解释链）：
- `fig_delta_plus1c_curve.png`（`X=base_t`，`Y=delta_plus_1c`，含 global/early/mid/late）
- `fig_treatment_distribution.png`（`X=treatment_value`，`Y=样本数或密度`，展示支持区间）
- `fig_outcome_by_stage.png`（`X=life_stage`，`Y=y_rel_drop`，展示阶段差异）
- `fig_weight_diagnostics.png`（`X=t_eval`，`Y=ESS与高分位权重`，展示重叠/稳定性）
5. 报告新增“图表解读”章节：
- 每张图固定三行说明：`X轴`、`Y轴`、`关键结论`，并在图下给出 1-2 句业务解释。
6. 输出目录保持为 [causal_initial_rate_effect](C:/Users/pal/projects/batt_soh/outputs/analysis/causal_initial_rate_effect)，仅覆盖同名结果文件，不新建外层目录。

### 直接执行：测试计划
1. 语言检查：报告正文不再出现英文模板章节（允许代码命令与字段ID保留英文）。
2. 参数覆盖检查：`report_parameter_sources.csv` 中参数集合应覆盖报告正文所有指标项。
3. 图文一致性检查：报告中每张图都必须有 `X/Y/关键结论` 三段说明，且图文件实际存在。
4. 数值一致性检查：报告中的全局与分阶段 `+1C` 数值与 `causal_effect_global.csv`、`causal_effect_by_stage.csv` 完全一致。
5. 稳定性检查：维持当前默认 `n_bootstrap=400` 跑通，无新增依赖安装需求。

### 深度交互：审慎挑战与默认取舍
- 你提出“每个参数来源都要解释”是正确方向；为避免报告过长，默认策略是：
1. 报告正文放“高可读摘要表”。
2. 完整追溯明细放 `report_parameter_sources.csv`（机器可检索、便于审计）。
- 图像增强会显著提升可解释性，但也会增加报告长度；默认保留“每图一句关键结论”，避免堆叠无效可视化。
- 本次不改变因果识别方法与样本口径，只做“中文化+可解释性增强”，确保结果可与当前版本直接横向对比。
[Response]
已完成实现：脚本增强、中文报告、参数来源追溯CSV、新增4张辅助图、默认400次bootstrap重跑与5项自检全部通过。
## 17:14:23

回顾下历史记录，确认下analysis目录为什么不在推送文件中

已确认：outputs/analysis 被 .gitignore 的 /outputs/ 规则忽略（第33行），该规则最早由提交 f8c9d60 引入，导致 analysis 目录内容不会进入暂存与推送。

